{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run Kotlin",
            "type": "shell",
            "command": "kotlinc interview_prep/main.kt -include-runtime -d hello.jar && java -jar hello.jar",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}